public interface FullAbstraction {
    void pay();
    void invoice();

    //these method are abstract method because interface gives full data abstraction
    //it is by default abstract method
    //in interface we cannot extends the class we can implements the class

    //in abstract class contain abstract or non abstract method
}
